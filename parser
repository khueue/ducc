#!/usr/bin/env escript
%%! -pa ebin

usage() ->
    io:format('Usage: parser~n'),
    io:format('(Uses stdin/stdout.)~n').

main([]) ->
    {ok, Tokens} = io_tools:term_from_input(),
    ParseTree = parser_driver:parse(Tokens),
    io_tools:term_to_output(ParseTree);
main(_) ->
    usage().
